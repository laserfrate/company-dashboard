{
  "name": "company-dashboard",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "heroku-postbuild": "NODE_OPTIONS=--openssl-legacy-provider npm install --prefix client && NODE_OPTIONS=--openssl-legacy-provider npm run build --prefix client"
  },
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "googleapis": "^140.0.0",
    "express": "^4.17.1",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
